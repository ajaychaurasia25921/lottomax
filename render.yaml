services:
  - type: web
    name: lottomax
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5180
      - key: LOTTOMAX_DATA_DIR
        value: /app/data
      - key: LOTTOMAX_PAYMENT_PROVIDER
        sync: false
      - key: LOTTOMAX_COMPANY_UPI_ID
        sync: false
      - key: LOTTOMAX_COMPANY_PAYEE_NAME
        sync: false
